//you can comment this file too by adding // before the comment
let greeting = "Hello, world!";
document.getElementById("greeting").innerHTML = greeting;

// variable 'greeting' takes a string value of "Hello, world!"
// the html element 'greeting' is inserted using .innerHTML property and takes the value of variable 'greeting'
// it's confusing that we use the same name for the variable and html element

//from now on all the HTML files will look the same so there is no need to comment them
